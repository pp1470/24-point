# 24-point
It is a project that has game and learn.The game mode uses cards game and the learn mode uses GLM-OCR mode to judge titles on the files that had been downloaded .Replacing '~~~' in python programs(app.py,ocr.py,picture_judge.py) with your api-key.
